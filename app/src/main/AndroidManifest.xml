<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodDeliveryService"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".activity.user.Dashbroads"
            android:exported="false" />

        <activity
            android:name=".activity.admin.HomeAdmin"
            android:exported="false" />
        <activity
            android:name=".activity.admin.UpdateFood"
            android:exported="false" />
        <activity
            android:name=".activity.admin.CreateNewProduct"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"/>

        <activity
            android:name=".activity.common.LoginActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activity.admin.ViewListFood"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.user.LogoutActivity" />

    </application>

</manifest>